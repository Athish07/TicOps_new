# Azure SQL Database profile
server.port=8080
spring.application.name=ticops-backend

# ── Azure SQL Database ───────────────────────────────────────────────
spring.datasource.url=jdbc:sqlserver://athish-sql-server.database.windows.net:1433;database=free-sql-db-3196819;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=CloudSAebeb205f
spring.datasource.password=Athish_main_07
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA / Hibernate for SQL Server
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.open-in-view=false

# JWT
app.jwt.secret=TicOps2026SuperSecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLong!!
app.jwt.expiration-ms=86400000

# CORS
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Email ingest API key (for Power Automate webhook)
app.email.ingest.api-key=TicOps-PA-Ingest-2026-SecretKey

# Disable email polling (not needed when using Power Automate)
app.email.polling.enabled=false

# Logging
logging.level.com.ticops=DEBUG
logging.level.org.springframework.security=WARN
